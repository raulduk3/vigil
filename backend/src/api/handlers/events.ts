/**
 * Actions Handler — V2
 *
 * Replaces the old events handler. Returns agent action log.
 * (This file is kept for import compat; logic lives in watchers.ts)
 */

export { } from "./watchers"; // re-export nothing — route handled in watchers.ts
