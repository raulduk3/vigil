/**
 * Tool Registry
 *
 * Built-in tools: send_alert, send_reply, update_thread, ignore_thread, webhook
 * Custom tools: user-defined webhook + schema
 *
 * See docs/V2_ARCHITECTURE.md for full spec.
 */

// TODO: Implement tool registry and handlers
export const BUILTIN_TOOLS = [];
