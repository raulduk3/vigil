/**
 * Agent Engine — V2 Core
 *
 * Replaces event-sourced runtime with LLM agent invocation.
 * Flow: load context → run LLM → execute tools → save memory
 *
 * See docs/V2_ARCHITECTURE.md for full spec.
 */

// TODO: Implement invocation flow
export async function invokeAgent(): Promise<void> {
  throw new Error('Not implemented — see V2_ARCHITECTURE.md');
}
